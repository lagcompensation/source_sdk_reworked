#include "globals.hpp"
#include "..\..\..\includes.hpp"

namespace g {
	csgo::c_usercmd* cmd = nullptr;
	bool sendpacket = true;
	vec3_t real_angle = vec3_t( 0, 0, 0 );
	vec3_t fake_angle = vec3_t( 0, 0, 0 );
}
